#ifndef CONFIG_HPP
#define CONFIG_HPP

#include <string>
#include <vector>
#include <map>
#include <unordered_map>
#include <queue>
#include <set>
#include <ctime>
#include <algorithm>
#include <iostream>
#include <fstream>

#include <openssl/sha.h>
#include <sstream>
#include <iomanip>
// #define SHA256_DIGEST_LENGTH 256
// // 通用结构体（可根据需要扩展）
// struct User {
//     std::string id;       // 用户 ID
//     std::string name;     // 用户名
//     Role role;            // 角色
//     std::string password; // 密码（建议加密存储）
// };

#endif // COMMON_HPP